# Plans Index

| Task | Slug | Status | Updated | Notes |
|---|---|---|---|---|
| MySQL container + Adminer UI (Docker Compose) | `mysql-docker-container` | complete | 2026-09-21 | Verified: MySQL 8.4.11 `healthy` on `127.0.0.1:3306` with data surviving `down`/`up`; Adminer login working on `127.0.0.1:8081`; verification table dropped. Environment limitation found and worked around (D8): user-defined Docker bridges drop container↔container traffic here, so Adminer uses `network_mode: host`. Deferred by decision (**D9**): no Laravel scaffold and no app container for now — layout options, version facts (Laravel 13, Filament 5.8.4, host PHP 8.4.15 missing `pdo_mysql`/`intl`) and the `network_mode: host` requirement are all recorded in the plan file, nothing was built |
| Laravel 13 application bootstrap (steps 1–2) | `laravel-app-bootstrap` | complete | 2026-09-22 | Created `laravel/laravel` v13.10.1 (framework v13.33.0) at the repository root by scaffolding in `/tmp` and copying with `rsync --ignore-existing` (guidelines library, `README.md`, `.gitignore` and `docker-compose.yml` untouched). `.env` set to `mysql` / `127.0.0.1:3306` / `personal_finances` / `laravel`; verified with `php artisan db:show` → MySQL 8.4.11, 0 tables. Verification ran in a local non-committed `pf-php:8.4-dev` image (`--user 1000:1000 --network host`) because host PHP lacks `pdo_mysql` (D11). |
| SQL schema import + Filament panel (steps 3–4) | `schema-import-and-filament` | complete | 2026-09-23 | Steps 3–4 verified (import + Filament v5.8.4 `/admin` panel + `AdminUserSeeder`; D13–D18). **Finding 1 resolved (D19):** the three financial-history FKs no longer cascade — `fk_transactions_account` → `RESTRICT`, `fk_transactions_category` → `SET NULL` (`transactions.category_id` now nullable), `fk_budgets_category` → `RESTRICT`; applied live and in `database/schema/01-personal-finances.sql`, proven by functional delete tests (T1/T3 refused with error 1451, T2 detached the transaction) and a scratch re-import with `DIFF_EXIT=0`; test rows cleaned up. **Still pending:** findings 2–8 — first the `fk_*_user` cascades + soft-delete policy (finding 2); then enum-vs-varchar, `month`/`year` checks, `amount` sign convention, transfers. |
| Duplicate `CHANGELOG.md` entry for 2026-09-22 | `changelog-dedupe-20260922` | pending | 2026-09-23 | Out-of-scope finding noticed while appending the 2026-09-23 entry: `docs/CHANGELOG.md` contains the "Laravel 13 application bootstrapped…" block twice in a row under the same date heading (lines ~41–61 and ~63–85). Not fixed (scope rules) — deleting the second copy is a one-block edit once confirmed. |
