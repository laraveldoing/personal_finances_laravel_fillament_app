# Plans Index

| Task | Slug | Status | Updated | Notes |
|---|---|---|---|---|
| MySQL container + Adminer UI (Docker Compose) | `mysql-docker-container` | complete | 2026-09-21 | Verified: MySQL 8.4.11 `healthy` on `127.0.0.1:3306` with data surviving `down`/`up`; Adminer login working on `127.0.0.1:8081`; verification table dropped. Environment limitation found and worked around (D8): user-defined Docker bridges drop container↔container traffic here, so Adminer uses `network_mode: host`. Deferred by decision (**D9**): no Laravel scaffold and no app container for now — layout options, version facts (Laravel 13, Filament 5.8.4, host PHP 8.4.15 missing `pdo_mysql`/`intl`) and the `network_mode: host` requirement are all recorded in the plan file, nothing was built |
| Laravel 13 application bootstrap (steps 1–2) | `laravel-app-bootstrap` | complete | 2026-09-22 | Created `laravel/laravel` v13.10.1 (framework v13.33.0) at the repository root by scaffolding in `/tmp` and copying with `rsync --ignore-existing` (guidelines library, `README.md`, `.gitignore` and `docker-compose.yml` untouched). `.env` set to `mysql` / `127.0.0.1:3306` / `personal_finances` / `laravel`; verified with `php artisan db:show` → MySQL 8.4.11, 0 tables. Verification ran in a local non-committed `pf-php:8.4-dev` image (`--user 1000:1000 --network host`) because host PHP lacks `pdo_mysql` (D11). |
| SQL schema import + Filament panel (steps 3–4) | `schema-import-and-filament` | complete | 2026-09-23 | Steps 3–4 verified (import + Filament v5.8.4 `/admin` panel + `AdminUserSeeder`; D13–D18). **Finding 1 resolved (D19):** the three financial-history FKs no longer cascade — `fk_transactions_account` → `RESTRICT`, `fk_transactions_category` → `SET NULL` (`transactions.category_id` now nullable), `fk_budgets_category` → `RESTRICT`; applied live and in `database/schema/01-personal-finances.sql`, proven by functional delete tests (T1/T3 refused with error 1451, T2 detached the transaction) and a scratch re-import with `DIFF_EXIT=0`; test rows cleaned up. **Finding 2 resolved 2026-09-25** (soft deletes + `RESTRICT` on the four `fk_*_user`, D20 in `user-delete-policy`). **Still pending:** findings 3–8 — enum-vs-varchar `status`, `month`/`year` checks, `amount` sign convention, transfers, `SET FOREIGN_KEY_CHECKS = 0`, singular/plural naming. |
| Duplicate `CHANGELOG.md` entry for 2026-09-22 | `changelog-dedupe-20260922` | complete | 2026-09-25 | Fixed 2026-09-25 as a one-block edit — trivial (diff describable in one sentence), so no `/plans` task file and no `CHANGELOG.md` entry of its own (`base/08-planning.md` rule 2, `documentation/changelog-and-docs.md`). Out-of-scope finding noticed while appending the 2026-09-23 entry: `docs/CHANGELOG.md` contains the "Laravel 13 application bootstrapped…" block twice in a row under the same date heading (lines ~41–61 and ~63–85). **Verified fix (2026-09-25):** the duplicated `## [2026-09-22]` heading and its block (lines 63–86) were deleted from `docs/CHANGELOG.md` — `git diff --numstat` reports `0` insertions / `24` deletions and the file went from 137 to 113 lines; `grep -c '^## [2026-09-22]$'` returns `1` and the remaining date headings are 2026-09-21 / 2026-09-22 / 2026-09-23. Re-runnable check: the file rebuilt as `HEAD` minus lines 63–86 diffs clean against the working copy (`DIFF_EXIT=0`), so nothing else changed. |
| User delete policy: `fk_*_user` cascades + soft deletes (finding 2) | `user-delete-policy` | complete | 2026-09-25 | **Option A implemented and verified (D20).** The four `fk_*_user` → `RESTRICT` live and in `database/schema/01-personal-finances.sql`; new migration `2026_09_25_000000_add_deleted_at_to_users_table` plus `SoftDeletes` on `App\Models\User`; `AdminUserSeeder` restores a trashed admin through `withTrashed()` instead of colliding with the unique `email`. Evidence: live `DELETE_RULE` = RESTRICT on all four, hard delete of a user with history refused with **error 1451** and the domain rows intact, live Eloquent soft delete reported `trashed=yes` / `find_after_delete=null` / `with_trashed_count=1`, scratch database re-import `DIFF_EXIT=0` (script idempotent; scratch and temporary grant removed), `php artisan test` 5 passed / 10 assertions, `pint --dirty --test` PASS (4 files). No purge path exists — by decision. |
| Remaining schema findings 3–8 (status, FK checks, ranges, sign, naming, transfers) | `schema-findings-3-8` | complete | 2026-09-25 | All six recommendations accepted and applied (D21–D26): `transactions.status` → `VARCHAR(20) DEFAULT 'completed'` with `App\Enums\TransactionStatus` as the list's home; both `SET FOREIGN_KEY_CHECKS` deleted; `chk_budgets_month` / `chk_budgets_year` / `chk_transactions_amount_not_zero` added live and in the script; sign convention written into the header; commented database lines now plural; transfers deferred with their shape recorded (D26). Evidence: live structure query shows `varchar(20)` + the three `CHECK` clauses with all eight FKs unchanged; `month = 13`, `month = 0`, `year = 1899`, `amount = 0` refused with **error 3819** while valid values and the `completed` default were stored; scratch import (no FK toggles) `DIFF_EXIT=0`, re-import clean; `php artisan test` 7 passed / 15 assertions; `pint --test` PASS. Accepted cost of D21: the database no longer enforces the status list. |
| Domain models + Filament resources for accounts/categories/transactions/budgets | `domain-models-and-filament-resources` | blocked | 2026-09-25 | Plan written before any code; blocked on the **ownership scope** (per-user scoping vs single-owner panel). Recommendations in the file: keep `accounts.balance` stored and editable (recalculating it needs ledger rules that do not exist yet) and no Filament policies until more than one user type exists. Scope: four Eloquent models plus four resources whose validation mirrors the settled database rules (D19–D24); out: dashboards, reports, import/export, transfers (D26), the purge path (D20) and any schema change. |

